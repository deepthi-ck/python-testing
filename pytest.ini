[pytest]
testpaths = tests
pythonpath = src
addopts = -q --strict-markers
markers =
    unit: isolated unit tests
    integration: database and service integration
    functional: end-to-end API journeys
